// Generated by CoffeeScript 1.9.0
(function() {
  $(document).ready(function() {
    var currentCharacter;
    $('#gender-identity-coords').gendermap().on('mapClick', function() {
      var mapData;
      mapData = $(this).data('gendermap');
      $('#gender-identity-coords--male').val(mapData.m);
      $('#gender-identity-coords--male-quantized').val(Math.ceil(mapData.m * 5));
      $('#gender-identity-coords--female').val(mapData.f);
      return $('#gender-identity-coords--female-quantized').val(Math.ceil(mapData.f * 5));
    });
    $('#gender-expression-coords').gendermap().on('mapClick', function() {
      var mapData;
      mapData = $(this).data('gendermap');
      $('#gender-expression-coords--male').val(mapData.m);
      $('#gender-expression-coords--male-quantized').val(Math.ceil(mapData.m * 5));
      $('#gender-expression-coords--female').val(mapData.f);
      return $('#gender-expression-coords--female-quantized').val(Math.ceil(mapData.f * 5));
    });
    $('#gender-in-furry-coords').gendermap().on('mapClick', function() {
      var mapData;
      mapData = $(this).data('gendermap');
      $('#gender-in-furry-coords--male').val(mapData.m);
      $('#gender-in-furry-coords--male-quantized').val(Math.ceil(mapData.m * 5));
      $('#gender-in-furry-coords--female').val(mapData.f);
      return $('#gender-in-furry-coords--female-quantized').val(Math.ceil(mapData.f * 5));
    });
    currentCharacter = 1;
    window.addCharacter = function() {
      $('#characters').append(Handlebars.templates.character({
        characterId: currentCharacter,
        isFirst: currentCharacter === 1
      }));
      return currentCharacter++;
    };
    return window.addCharacter();
  });

}).call(this);
