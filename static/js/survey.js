// Generated by CoffeeScript 1.9.0
(function() {
  var markQuestionAnswered, unlockOnOther;

  markQuestionAnswered = function(number) {
    return $.getJSON("/touch/question/" + number, function(data) {
      if (data.error) {
        return console.error(data.error);
      }
    });
  };

  unlockOnOther = function(name, enabled) {
    return $("input[name=" + name + "_other]").prop('disabled', !enabled);
  };

  $(document).ready(function() {
    $('input,textarea').on('blur', function() {
      if ($(this).data('number')) {
        return markQuestionAnswered($(this).data('number'));
      }
    });
    return $('input[value=other]').on('change', function() {
      return unlockOnOther($(this).attr('name'), $(this).prop('checked'));
    });
  });

}).call(this);
